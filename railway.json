{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && (node create-project-sessions-table.js || true) && npm run build"
  },
  "deploy": {
    "startCommand": "npx drizzle-kit push || true; npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}